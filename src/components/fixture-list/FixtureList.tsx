import { fixtureItems } from '@/app/data/constants';

type Props = {};

const FixtureList = (props: Props) => {
  const fixture = fixtureItems;
  // TODO Look at adding react-window for performance and infinite loading
  return <div></div>;
};

export default FixtureList;
